// function createRandomGroups (pool, groupSize) {
//   let groups = [];
//   let groupNumber = 1

//   shuffle(pool);

//   while (pool.length > 0) {
//     if (groups[`group${groupNumber}`])
//   }
// }

// function shuffle(arr) { // Fisher-Yates shuffle
//   // adds the first student from the pool to a new group	  for (let i = arr.length - 1; i > 0; i--) {
//   // deletes that first student from the pool	    let j = Math.floor(Math.random() * (i + 1));
//   groups[`group${groupNumber}`]= [pool[0]];	    [arr[i], arr[j]] = [arr[j], arr[i]];
//   }
//   return arr;
// }

// export default createRandomGroups;